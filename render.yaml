# Render blueprint. Deploy API + worker from one repo.
# Managed Postgres + Redis are provisioned here; to use Neon/Upstash instead,
# set DATABASE_URL / REDIS_URL to sync:false and paste the external URLs.
databases:
  - name: medassist-db
    postgresMajorVersion: "16"
    plan: free

services:
  - type: redis
    name: medassist-redis
    plan: free
    ipAllowList: []

  - type: web
    name: medassist-api
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: medassist-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: medassist-redis
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: ENFORCE_HTTPS
        value: "true"
      - key: COOKIE_SECURE
        value: "true"
      # For a split frontend origin (e.g. Vercel) set COOKIE_SAMESITE=none.
      - key: COOKIE_SAMESITE
        value: lax
      - key: OPENAI_API_KEY
        sync: false
      - key: OPENAI_CHAT_MODEL
        value: gpt-4o-mini
      - key: OPENAI_EMBED_MODEL
        value: text-embedding-3-small
      - key: STORAGE_ENDPOINT
        sync: false
      - key: STORAGE_REGION
        value: auto
      - key: STORAGE_BUCKET
        sync: false
      - key: STORAGE_ACCESS_KEY
        sync: false
      - key: STORAGE_SECRET_KEY
        sync: false
      - key: CORS_ORIGINS
        sync: false

  - type: worker
    name: medassist-worker
    runtime: docker
    dockerfilePath: ./backend/Dockerfile.worker
    dockerContext: ./backend
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: medassist-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: medassist-redis
          property: connectionString
      - key: OPENAI_API_KEY
        sync: false
      - key: OPENAI_CHAT_MODEL
        value: gpt-4o-mini
      - key: OPENAI_EMBED_MODEL
        value: text-embedding-3-small
      - key: STORAGE_ENDPOINT
        sync: false
      - key: STORAGE_REGION
        value: auto
      - key: STORAGE_BUCKET
        sync: false
      - key: STORAGE_ACCESS_KEY
        sync: false
      - key: STORAGE_SECRET_KEY
        sync: false
