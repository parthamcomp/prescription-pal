-r requirements.txt

# Test runner
pytest==8.3.4
pytest-asyncio==0.25.2
pytest-xdist==3.6.1          # parallel + --randomly-seed order randomization (via pytest-randomly)
pytest-randomly==3.16.0
pytest-cov==6.0.0

# Tier 2/3 real-engine fixtures
testcontainers[postgres]==4.9.0

# Tier 1 doubles
fakeredis==2.26.2
freezegun==1.5.1

# Property-based testing
hypothesis==6.123.2

# Mutation testing (Phase 3 verification)
mutmut==3.2.2

# Contract tests
deepdiff==8.1.1               # structural diff for the OpenAPI snapshot test
